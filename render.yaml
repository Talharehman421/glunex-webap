services:
  - type: web
    name: glunex-webapp
    runtime: docker
    # Must live at repo root; Docker context must be repo root (see Dockerfile header).
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    plan: free
    healthCheckPath: /health
