# Unified deployment: the API lives on the same host as this app.
# Do not set REACT_APP_API_URL here unless the UI is hosted separately from FastAPI.
#
# Example (split hosting only):
# REACT_APP_API_URL=https://your-api.example.com
