# Local dev: `package.json` "proxy" forwards /predict and /predict/gene to FastAPI :8000.
# Start backend: cd backend && uvicorn main:app --reload
#
# Optional: call the API directly instead of the proxy:
# REACT_APP_API_URL=http://127.0.0.1:8000
